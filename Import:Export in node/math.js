const pi=3.14;
const add=(a,b)=> {return (a+b) };
const sub=(a,b)=> {return (a-b) };
const mul=(a,b)=> {return (a*b) };
const div=(a,b)=> {return (a/b) };

const obj={
    pi : pi,
    add : add,
    sub : sub,
    mul : mul,
    div : div
}

module.exports = obj;